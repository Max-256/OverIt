import React from "react";
import { View, Text } from "react-native";

import Screen from "../components/Screen";

function RegisterScreen(props) {
  return (
    <Screen>
      <Text>Regiser Screen</Text>
    </Screen>
  );
}

export default RegisterScreen;
